import { describe, it, expect } from 'vitest';

describe('Example test', () => {
  it('should pass', () => {
    expect(1 + 1).toBe(2);
  });

  it('should check true', () => {
    expect(true).toBe(true);
  });
});